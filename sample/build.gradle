version = '0.1.0'

asciidoctor {
  sources {
    include 'sample.adoc', '日本語サンプル.adoc'
  }
  resources {
    from(sourceDir) {
      include 'images/**'
    }
  }
}

asciidoctorj {
  attributes(
    revnumber: project.version,
  )
}
